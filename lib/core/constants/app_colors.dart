import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color primary = Color(0xFF0066CC);
  static const Color background = Color(0xFF003399);
  static const Color surface = Colors.white;
  static const Color cancel = Color(0xFFCC0000);
  static const Color confirm = Color(0xFF006633);
  static const Color clear = Color(0xFFFF9900);
}
